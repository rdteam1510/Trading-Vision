import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
    loading_spinner:{
        marginTop: "20%",
        marginLeft: "50%",
        marginBottom: "40%",
    }
}))

export default useStyles